# Optional: override backend API base URL
# If omitted, frontend uses default http://localhost:5000/api
REACT_APP_API_URL=http://localhost:5000/api
# Optional: set true only for demo/training data seeding on first load
REACT_APP_ENABLE_DEMO_MEDICINE_SEED=false
